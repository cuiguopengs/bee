server:
  port: 7002

spring:
  application:
    name: config-client
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:7001

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9001/eureka/
